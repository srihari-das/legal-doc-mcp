# PDF Compliance Analyzer MCP Server
# No API keys needed - this server runs locally with PyMuPDF

# Port for HTTP transport (set automatically by Dedalus during deployment)
# PORT=8000

# Optional: Set log level
# LOG_LEVEL=INFO
